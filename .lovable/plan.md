
Replace the "VolCop." text logo in `src/components/marketing/marketing-header.tsx` with the **Stacked civic mark**: two lines "VOL" / "COP" in heavy uppercase Public Sans with wide letter-spacing, separated by a thin gold (`#B48A44`) horizontal rule. The Link wrapper, hover behavior, and `to="/"` stay intact.

No other files change. The authed `site-header.tsx` keeps its CopSmart shield mark since it's a different surface.

Files to change:
- `src/components/marketing/marketing-header.tsx` — swap the wordmark span for the stacked block.
